apiVersion: apps/v1
kind: Deployment

metadata:
  name
  literals:
    - SERVICE_NAME=app-banco-bd
  replicas: 1
  selector:
    matchLabels:
      app: next-app
  template:
    metadata:
      labels:
        app: next-app
    spec:
      containers:
        - name: next-app
          image: next-app:latest
          ports:
            - containerPort: 3000

            tem que mudar muita coisa aqui no deployment 